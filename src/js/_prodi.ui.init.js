var prodi = prodi || {ui:{}};

/**
 * Function executed when document is ready. It calls all modules' init function.
 */
$(function () {
    /**
     * Inits Sample Module #1
     */
    prodi.ui.todoList.init();

});
